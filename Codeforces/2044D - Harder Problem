#include <iostream>
#include <vector>
     
using namespace std;

int main(){
    int quant;
    cin >> quant;
    
    for (int i = 0; i < quant; ++i){
		int n;
		cin >> n;

		vector<int> vetor(n + 1);
		int mais = 0;

		for (int j = 0; j < n; ++j){
			int atual;
			cin >> atual;

			++vetor[atual];

			if (vetor[atual] > vetor[mais])
				mais = atual;

			if (j == n - 1)
				cout << mais << endl;
			else	
				cout << mais << " ";
		}

    }
    
    return 0;
}
